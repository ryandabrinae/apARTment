$('.newArtButton').click(function(event) {
  /* Act on the event */
});

const searchArt = function(response) {
  const newImage = response.img_url;
};

const appendArt = function(newImage){
  $('.artDiv').remove();
  constNewArtDiv = $('<div class="artDiv">');
  const $addButton = $('<button class="add-button"> Add New Beer </button>').click(() => {

  }
};
